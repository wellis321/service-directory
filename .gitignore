.env
.env.local
.env.*.local
.DS_Store

# Runtime data (CSV archives, logs, admin metrics) — regenerated by cron/import.php,
# not source. Directories are created automatically by the app if missing.
/storage/

# Credentials for the /admin/ Basic Auth — never commit, path also differs per environment
/admin/.htpasswd

# Unrelated local tool artifacts / caches, not part of this project
/data/
/carescotland/
/.deepsec/
/.fallow/
